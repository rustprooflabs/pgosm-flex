""" Unit tests to cover the DB module."""
import unittest
import osm2pgsql_recommendation


class Osm2pgsqlRecommendationTests(unittest.TestCase):

    def test_something_does_the_right_thing(self):
        result = 'Not Real'
        expected = False
        self.assertEqual(expected, result)
